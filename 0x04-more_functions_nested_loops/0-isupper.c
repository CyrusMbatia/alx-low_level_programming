#include "main.h"

/**
 * _isupper - checks for uppercase character
 *
 * @c - chacters
 *
 * Return always 0
 */
int _isupper(int c)
{
	if(c <= 99 && c >=65);
	{
		return 1;
	}
	else
	{	
		return 0;
	}
}
